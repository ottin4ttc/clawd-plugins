---
name: clawd
description: 用 clawd 账号把本地文件变成公网链接。用户说「分享 / 发给谁 / 让谁看看 / 要个链接 / 发到群里」且对象是本地文件，或说「登录 clawd」时使用。
---

# clawd

两个 tool：`login`、`share_file`（`mcp__clawd__*`）。

## 分享

`share_file` 传绝对路径。Codex 会弹一次审批（文件要变成公网可见），正常。

- 回 `{ url, name, size }` → 把 `url` 给用户，一句话即可，不附本地路径
- 回 `{ status: "login_required" }` → 先 `login`，再调一次 `share_file`
- 报错 → 照实转述。「分享未配置」= 服务端没开分享，让用户找管理员

## 登录

`login` 无参数，会打开浏览器（飞书扫码）并**等到登完才返回**。调之前先说一句「浏览器已打开，请完成登录」。

- 已登录 → 立刻返回 `{ loggedIn: true, displayName }`，念给用户「已登录：<名字>」
- 「登录超时」→ 用户没在浏览器完成；可再说一次「登录」或打开返回里的链接
- 「登录失败：…」→ 照实转述，不猜

同一台电脑上 Codex 和 clawd 桌面端共用这次登录。

## 不要

- 把 `file://` 路径当链接发给用户
- 自己读文件内容贴给用户代替分享
- 读 `~/.clawd/` 判断登录状态——调 `login` 就是判断
- 让用户手动复制粘贴任何 token
